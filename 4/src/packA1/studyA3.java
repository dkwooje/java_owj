package packA1;
/*
import java.io.IOException;
import java.io.OutputStream;

public class studyA3 {
	public static void main(String[] args)  throws IOException {
		OutputStream os = System.out;
		
		//1. 1-byte 단위 쓰기
		os.write('J');
		os.write('A');
		os.write('V');
		os.write('A');
		os.write('\r');  //13(생략 가능)
		os.write('\n');  // 10 개행
		os.flush();
		
		//2. N-byte 단위 쓰기 (byte[]의 처음부터 끝까지 데이터 쓰기)
		byte[] byteArray1 = "Hello!".getBytes();
		os.write(byteArray1);
		os.write('\n');
		os.flush();
		
		//3.  N-byte 단위 쓰기 (byte[]의 offset부터 length개의 byte 데이터 쓰기)
		byte[] byteArray2 = "Bettet the last smile than the first laughter".getBytes();
		os.write(byteArray2, 7, 8 );
		os.flush();
		
		

	}
}
*/